<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dlink.mapper.RoleResourceMapper">
    <sql id="baseSql">
        id,role_id,resource_id
    </sql>
    <select id="selectForProTable" resultType="com.dlink.model.RoleResource">
        select
        <include refid="baseSql"/>
        from
        dlink_role_resource
        <where>
            1=1
            <if test='param.roleId!=null and param.roleId!=""'>
                and role_id = #{param.roleId}
            </if>
            <if test='param.resourceId!=null and param.resourceId!=""'>
                and resource_id = #{param.resourceId}
            </if>
            <if test='ew.sqlSegment!=null and ew.sqlSegment!="" and !ew.sqlSegment.startsWith(" ORDER BY")'>
                and
            </if>
            <if test='ew.sqlSegment!=null and ew.sqlSegment!=""'>
                ${ew.sqlSegment}
            </if>
        </where>
    </select>
</mapper>
